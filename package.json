{
  "name": "@localizesh/processor-mdx",
  "repository": "localizesh/processor-mdx",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc --project tsconfig.build.json",
    "test": "mocha",
    "postinstall": "patch-package-ignore-missing --patch-dir node_modules/@localizesh/processor-md/patches && patch-package-ignore-missing --ignore-missing"
  },
  "dependencies": {
    "@localizesh/processor-md": "^0.2.2",
    "remark-mdx": "^3.1.0"
  },
  "devDependencies": {
    "@types/chai": "^5.0.1",
    "@types/mocha": "^10.0.7",
    "chai": "^5.1.2",
    "eol": "^0.10.0",
    "mocha": "^11.0.1",
    "patch-package-ignore-missing": "^7.0.1-2",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "peerDependencies": {
    "@localizesh/sdk": "^0.2.0"
  }
}
